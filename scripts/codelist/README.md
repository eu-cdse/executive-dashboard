# Generates cl_sat_product codelist and upload it to cloudferro bucket

## Steps

1. Add `codelist.xlsx` to `generate` folder
2. Create new `.env` in `upload` folder
3. Move to `scripts/codelist` folder if not already.
4. Run `deploy_to_s3.sh`
